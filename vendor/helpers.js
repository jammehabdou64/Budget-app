const path = require("path");

// function getController(controllerName) {
//   return require("../app/controller/" + controllerName);
// }
// function view({ res, dir, file }) {
//   if (!dir) {
//     dir = "../views";
//   }
//   if (!file) {
//     file = "index.html";
//   }
//   return res.sendFile(path.join(__dirname, dir, file));
// }

// global.getController = getController;
